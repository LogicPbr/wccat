spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:h2:mem:wccat;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL;DATABASE_TO_LOWER=TRUE

logging:
  level:
    com.wccat: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
